export function SectionLabel({ children }: { children: string }) {
  return (
    <span className="mb-4 block text-[11px] font-semibold uppercase tracking-[4px] text-teal">
      {children}
    </span>
  );
}
